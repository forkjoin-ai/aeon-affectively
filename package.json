{
  "name": "@a0n/aeon-affectively",
  "version": "0.1.0",
  "description": "Personality and emotion tracking as void walking over complement distributions",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "NODE_OPTIONS=--max-old-space-size=8192 tsc",
    "test": "bun test --timeout 30000 --pass-with-no-tests"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
